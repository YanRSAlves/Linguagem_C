#include <stdio.h>
#include <stdlib.h>

int main()
{





    // ax2 + bx + c = 0
    // delta = b2 - 4*a*c
    // x1 e x2 = -b +- RaizQ(delta)/2*a

    // delta = 0, temos uma raiz real.
    // delta > 0, temos duas raizes reais.
    // delta < 0, nao temos raizes reais.






    return 0;
}
